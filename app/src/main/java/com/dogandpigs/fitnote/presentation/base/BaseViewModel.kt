package com.dogandpigs.fitnote.presentation.base

import androidx.lifecycle.AndroidViewModel
import com.dogandpigs.fitnote.core.FNApplication

open class BaseViewModel(application: FNApplication) : AndroidViewModel(application) {
}